@model MeslekiPrj.Models.Product
<li>
    <figure>
        <a class="aa-product-img" href="@Url.Action("ViewDetails", "Product1", new { id = Model.ProductID })">
            <img src="@Url.Content(Model.PicturePath)" alt="" class="img-fluid img-thumbnail" >
        </a>
        <a class="aa-add-card-btn" href="@(Session["username"] != null ? Url.Action("AddToCart", "Product1", new { id = @Model.ProductID}) : Url.Action("Login", "Account"))"><span class="fa fa-shopping-cart"></span>Sepete Ekle</a>
        <figcaption>
            <h4 class="aa-product-title"><a href="#">@Model.Name</a></h4>
            <span class="aa-product-price">@Model.UnitPrice TL</span><span class="aa-product-old-price">
                <del>
                    @if (Model.OldPrice != null)
                    {
                        <text> @Model.OldPrice TL</text>
                    }
                </del>
            </span>
        </figcaption>
    </figure>
    <div class="aa-product-hvr-content">
        <a href="@(Session["username"]!=null ? Url.Action("WishList","Product1", new {id=@Model.ProductID }) :Url.Action("Login","Account"))" data-toggle="tooltip" data-placement="top" title="İstek Listesine Ekle"><span class="fa fa-heart-o"></span></a>
        <a href="@Url.Action("ViewDetails", "Product1", new { id = Model.ProductID })" title="Detayları Görüntüle" data-toggle="tooltip" data-placement="top"><span class="fa fa-info"></span></a>
    </div>
</li>



