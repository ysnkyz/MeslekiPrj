@model IEnumerable<MeslekiPrj.Models.Product>
@{
    ViewBag.Title = "Ürün Bilgisi";
}

<div class="row">
    <div class="col">
        <h2 class="text-info">Ürün Bilgisi</h2>
        <hr />
        <div class="d-flex justify-content-end mb-2">
            <a href="@Url.Action("Create", "Product")" class="btn btn btn-outline-primary"><i class="fa-regular fa-square-plus fs-1"></i>Yeni Ürün Ekle</a>
        </div>
        <div class="table-responsive">
            <table class="table table-condensed table-hover table-bordered table-striped table">
                <thead>
                    <tr>
                        @*<th>@Html.DisplayNameFor(p => p.Name)</th>*@
                        <th>Ürün Adı</th>
                        @*<th>@Html.DisplayNameFor(p => p.SupplierID)</th>*@
                        <th>Tedarikçi</th>
                        @*<th>@Html.DisplayNameFor(p => p.CategoryID)</th>*@
                        <th>Kategori</th>
                        @*<th>@Html.DisplayNameFor(p => p.SubCategoryID)</th>*@
                        @*<th>@Html.DisplayNameFor(p => p.UnitPrice)</th>*@
                        <th>Birim Fiyatı</th>
                        @*<th>@Html.DisplayNameFor(p => p.UnitInStock)</th>*@
                        <th>Aktif Stok</th>
                        <th>Eylemler</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var pdt in Model)
                    {
                        <tr>
                            <td>@Html.DisplayFor(p => pdt.Name)</td>
                            <td>@Html.DisplayFor(p => pdt.Supplier.CompanyName)</td>
                            <td>@Html.DisplayFor(p => pdt.Category.Name)</td>
                            @*<td>@Html.DisplayFor(p => pdt.SubCategory.Name)</td>*@
                            <td>@Html.DisplayFor(p => pdt.UnitPrice)</td>
                            <td>@Html.DisplayFor(p => pdt.UnitInStock)</td>
                            <td>
                                <a href="@Url.Action("Edit", "Product", new {id = pdt.ProductID })" class="btn btn-outline-warning btn-sm"><i class="fa-solid fa-pen-to-square fs-4"></i>Düzenle</a>
                                <a href="@Url.Action("Details", "Product", new {id = pdt.ProductID })" class="btn btn-outline-info btn-sm"><i class="fa-regular fa-circle-info fs-4"></i>Detaylar</a>
                                <a href="@Url.Action("Delete", "Product", new {id = pdt.ProductID })" class="btn btn-outline-danger btn-sm"><i class="fa-regular fa-trash-arrow-up fs-4"></i>Sil</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

